export interface Quote {
     id: string;
     text: string;
     author: string;
     authorUsername: string;
     submittedAt: string;
     approved: boolean;
     featured: boolean;
     featuredDate: string;
     score: number;
}